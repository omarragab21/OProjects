import 'package:flutter/material.dart';

class DeletedScreenLayout extends StatelessWidget {
  const DeletedScreenLayout({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}